import { Octokit } from "@octokit/rest";
export function getClient(token, userAgent, apiUrl) {
    return new Octokit({
        auth: token,
        userAgent: userAgent || `GitHub Actions Language Server`,
        baseUrl: apiUrl
    });
}
//# sourceMappingURL=client.js.map