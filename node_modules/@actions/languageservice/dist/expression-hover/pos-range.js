export function posWithinRange(pos, range) {
    return (pos.line >= range.start.line &&
        pos.line <= range.end.line &&
        pos.column >= range.start.column &&
        pos.column <= range.end.column);
}
//# sourceMappingURL=pos-range.js.map